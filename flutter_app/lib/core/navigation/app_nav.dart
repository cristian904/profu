import "package:flutter/material.dart";

/// Global navigator key so any screen can trigger root navigation (e.g. after Google sign-in).
final GlobalKey<NavigatorState> appNavigatorKey = GlobalKey<NavigatorState>();
