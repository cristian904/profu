[project]
name = "profu-logging"
version = "1.0.0"
description = "FastAPI logging utilities"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.package-dir]
"profu_logging" = "profu_logging"

[tool.setuptools]
packages = ["profu_logging"]
