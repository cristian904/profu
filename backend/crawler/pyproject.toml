[tool.poetry]
name = "crawler"
version = "1.0.0"
description = "Crawler for bacalaureat M1 PDFs from variante-mate.ro"
authors = ["Your Name <your.email@example.com>"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
httpx = "^0.27.0"
beautifulsoup4 = "^4.12.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
